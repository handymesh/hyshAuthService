apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hyshauthservice.fullname" . }}-test-connection"
  labels:
    {{- include "hyshauthservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hyshauthservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
